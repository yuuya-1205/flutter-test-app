# テスト設計書（テンプレート）

> このファイルは雛形です。`docs/` 配下などにコピーし、`<...>` のプレースホルダを
> 埋めて使ってください。記入のヒントは `<!-- ... -->` コメントに書いています。記入後は
> コメント行を削除して構いません。
>
> この設計書を埋めて渡してもらえれば、エージェント（Claude Code など）が
> skill **bloc-test** / **test-workflow** に沿ってテストを実装します。

---

## 1. メタ情報

| 項目 | 内容 |
| --- | --- |
| テスト対象 | `<対象クラス/ファイル。例: CounterBloc (lib/counter/counter_bloc.dart)>` |
| テスト種別 | `<unit / widget / integration のいずれか・複数>` |
| 作成者 | `<氏名>` |
| 作成日 | `<YYYY-MM-DD>` |
| 関連 Issue / PR | `<#番号 など。なければ「なし」>` |

## 2. 目的・スコープ

<!-- このテストで「何を保証したいか」を1〜3行で。 -->

- 目的：`<例: カウンターの加算・減算が状態へ正しく反映されることを保証する>`
- 対象範囲（In Scope）：`<テスト対象に含めるもの>`
- 対象外（Out of Scope）：`<テストしないもの。例: ネイティブ層、外部APIなど>`

## 3. 前提条件 / 準備

<!-- テスト実行に必要な初期状態・モック・テストデータなど。 -->

- `<例: CounterBloc は引数なしで生成し、初期状態は CounterState(count: 0)>`
- `<例: 外部依存はないためモックは不要>`

## 4. テスト観点

<!-- 洗い出した観点にチェック。該当しないものは「対象外」に理由を書く。 -->

- [ ] 正常系（期待どおりの入力・操作）
- [ ] 異常系（不正な入力・エラー経路）
- [ ] 境界値（0、最小・最大、空、上限/下限の前後）
- [ ] 状態遷移（イベントによる State の変化、初期状態、連続操作）
- [ ] UI 表示（Widget の表示・操作。widget テストの場合）
- [ ] その他：`<必要なら追記>`

## 5. テストケース一覧

<!--
1行＝1テストケース。種別は unit/widget。優先度は H/M/L。
「期待結果」は具体的に（数値・State・表示文言など）書く。
-->

| ID | 観点 | 前提（状態） | 操作 / 入力 | 期待結果 | 種別 | 優先度 |
| --- | --- | --- | --- | --- | --- | --- |
| TC-01 | 正常系 | `<例: count = 0>` | `<例: CounterIncremented を add>` | `<例: count = 1>` | unit | H |
| TC-02 | 境界値 | `<...>` | `<...>` | `<...>` | unit | M |
| TC-03 | 状態遷移 | `<...>` | `<...>` | `<...>` | unit | H |
| TC-04 | UI 表示 | `<...>` | `<例: + ボタンをタップ>` | `<例: "1" が表示される>` | widget | H |

## 6. 品質メトリクス目標

<!-- skill test-workflow に準拠。プロジェクト既定はカバレッジ100%。 -->

| 指標 | 目標 |
| --- | --- |
| 計画テストケース数 | `<件数>` |
| 観点カバレッジ | 100%（洗い出した観点をすべてカバー） |
| ラインカバレッジ | 100% |
| ブランチカバレッジ | 100% |

## 7. 補足・備考

<!-- レビュー時の注意点、テスト不能な箇所とその理由（coverage:ignore の有無）など。 -->

- `<例: lib/main.dart の main() はカバレッジ対象外（CIで除外済み）>`
- `<その他特記事項>`
